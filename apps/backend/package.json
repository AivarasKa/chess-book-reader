{
  "name": "chess-book-reader-backend",
  "private": true,
  "version": "0.1.0",
  "description": "Wrapper scripts for the FastAPI backend (Python)",
  "scripts": {
    "setup": "python -m venv .venv && .venv\\Scripts\\python -m pip install --upgrade pip && .venv\\Scripts\\python -m pip install -r requirements.txt",
    "dev": ".venv\\Scripts\\python -m uvicorn app.main:app --host 127.0.0.1 --port 8123 --reload"
  }
}
